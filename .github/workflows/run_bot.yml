- name: Save files back to GitHubrun: |
        git config --global user.name "Sentiment Bot"
        git config --global user.email "actions@github.com"
        git checkout main
        git add *.csv
        git commit -m "Automated daily CSV sentiment update" || exit 0
        git push origin main

